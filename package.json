{
  "name": "@daaku/firebase-auth",
  "author": "Naitik Shah <n@daaku.org>",
  "version": "2.4.0",
  "description": "Firebase Auth token management.",
  "repository": "git@github.com:daaku/firebase-auth",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.mjs"
    }
  },
  "scripts": {
    "prepare": "mkdir -p dist && dak-transpile < index.dak > dist/index.mjs",
    "test": "dak test/build.dak && TIMEOUT=30000 qunit-puppeteer"
  },
  "keywords": [
    "firebase",
    "auth"
  ],
  "devDependencies": {
    "@daaku/qunit-puppeteer": "^3.0.0",
    "@daklang/cli": "^0.9.2",
    "@daklang/esbuild": "^0.1.2",
    "esbuild": "^0.17.12",
    "nanoid": "^4.0.0",
    "qunit": "^2.17.2"
  }
}
